name: sfgen
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.7
  - pytorch=1.5.1
  - cudatoolkit=10.2
  - numpy
  - psutil
  - opencv  # atari.
  - pip
  - blosc
  - jupyterlab==2.2.6
  - matplotlib
  - pandas
  - tqdm
  - tensorboard
  - pip:
    - ipdb
    - atari-py
    - pyprind  # progbar.
    - gym
    - scikit-build
    - sklearn
    - tensorboardx
# conda env create -f gpu.yaml
# conda remove --name sfgen --all
