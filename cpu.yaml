name: sfgen
channels:
  - pytorch
  - defaults
  - conda-forge
dependencies:
  - python=3.7
  - pytorch=1.5.1
  - numpy
  - psutil
  - opencv  # atari.
  - pip
  - blosc
  - ffmpeg
  - matplotlib
  - pip:
    - ipdb
    - atari-py
    - pyprind  # progbar.
    - gym
    - scikit-build
    - sklearn

# conda env create -f cpu.yaml
# conda remove --name sfgen --all
